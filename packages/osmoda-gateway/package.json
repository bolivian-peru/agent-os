{
  "name": "osmoda-gateway",
  "version": "1.3.0",
  "description": "osModa Gateway — modular runtime (claude-code + openclaw 2026.5+ drivers), gateway-owned canonical transcript (JSONL per session) + durable MEMORY.md auto-load + transcript re-seed on session loss, disk-persisted runtime-tagged sessions, healthCheck-gated swaps, two-text-channel streaming contract (interim_text thinking + text_bulk final answer) honored by BOTH drivers, one assistant transcript entry per turn, tool-target hints, 92 MCP tools + optional CodeGraph code-intelligence, hot-reloadable agents.json",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "osmoda-gateway": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "test": "node --test test/*.test.js",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "ws": "^8.21.0"
  },
  "optionalDependencies": {
    "@anthropic-ai/claude-code": "^2.1.175"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/ws": "^8.5.0",
    "typescript": "^5.6.0"
  }
}
